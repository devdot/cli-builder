#!/bin/bash

if [ "$1" == "" ]; then
    echo "You need to supply version as argument this script!"
    exit
fi

bin/dev build:readme
bin/dev build:phar --build-version=$1
rm bin/cli-builder
mv cli-builder.phar bin/cli-builder

git add README.md
git add bin/cli-builder
